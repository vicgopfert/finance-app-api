name: Deploy

on:
    push:
        branches:
            - main

jobs:
    check:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            - name: Use Node.js
              uses: actions/setup-node@v4
              with:
                  node-version: '20'

            - name: Install dependencies
              run: npm ci

            - name: Start PostgreSQL Test Container
              run: docker compose up -d postgres-test

            - name: Wait for PostgreSQL
              run: |
                  for i in {1..30}; do
                    docker compose exec postgres-test pg_isready -U postgres && exit 0
                    sleep 1
                  done
                  exit 1

            - name: Apply migrations
              env:
                  DATABASE_URL: postgresql://postgres:test_password@localhost:5433/financeapp
              run: npx prisma migrate deploy

            - name: ESLint
              run: npm run eslint:check

            - name: Prettier
              run: npm run prettier:check

            - name: Test
              env:
                  DATABASE_URL: postgresql://postgres:test_password@localhost:5433/financeapp
              run: npm run test

            - name: Stop Containers
              if: always()
              run: docker compose down

    migrate:
        runs-on: ubuntu-latest
        needs: check
        environment: production

        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            - name: Use Node.js
              uses: actions/setup-node@v4
              with:
                  node-version: '20'

            - name: Install dependencies
              run: npm ci

            - name: Run migrations
              env:
                  DATABASE_URL: ${{ secrets.DATABASE_URL }}
              run: npx prisma migrate deploy
